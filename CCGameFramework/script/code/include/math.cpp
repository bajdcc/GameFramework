//
// Project: clibparser
// Created by bajdcc
//

// 数学函数（为了精度，默认先做double）
double sqrt(double number) {
    number;
    interrupt 201;
}

// 返回[0,count)间的随机数
int rand(int count) {
    count;
    interrupt 202;
}